import React from 'react';
import { ScrollIntoViewAPI } from './api';
const { Provider, Consumer, } = React.createContext(null);
export const APIConsumer = Consumer;
export class ProvideAPI extends React.Component {
    constructor() {
        super(...arguments);
        this.api = new ScrollIntoViewAPI(this.props.dependencies);
    }
    render() {
        return (React.createElement(Provider, { value: this.api }, this.props.children));
    }
}
//# sourceMappingURL=context.js.map